@{
    var fullName = User.FindFirst("full_name")?.Value ?? User.Identity?.Name ?? "User";
}

<div class="page-hero p-4 p-md-5 rounded-4">
    <p class="text-uppercase fw-semibold small mb-2">SINARA ERP</p>
    <h1 class="display-6 fw-bold mb-3">@string.Format(T["Welcome, {0}"], fullName)</h1>
    <p class="mb-0 text-secondary">@T["Phase 4 active: dynamic RBAC navigation, EN/ID i18n, and basic system configuration module are running."]</p>
</div>
