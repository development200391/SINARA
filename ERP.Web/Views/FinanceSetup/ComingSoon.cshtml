@model string

<div class="card border-0 shadow-sm">
    <div class="card-body p-4">
        <h4 class="mb-2">@Model</h4>
        <p class="text-muted mb-0">Fase 8 Finance API sudah aktif. Halaman UI detail bisa dilanjutkan di tahap berikutnya.</p>
    </div>
</div>
