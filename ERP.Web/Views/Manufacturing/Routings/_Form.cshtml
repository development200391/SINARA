@model ERP.Web.ViewModels.Manufacturing.ManufacturingRoutingEditViewModel

<div class="card border-0 shadow-sm">
    <div class="card-body">
        <div class="row g-3">
            <div class="col-md-3">
                <label asp-for="Code" class="form-label"></label>
                <input asp-for="Code" class="form-control" />
                <span asp-validation-for="Code" class="text-danger"></span>
            </div>
            <div class="col-md-5">
                <label asp-for="Name" class="form-label"></label>
                <input asp-for="Name" class="form-control" />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>
            <div class="col-md-2">
                <label asp-for="Version" class="form-label"></label>
                <input asp-for="Version" class="form-control" type="number" min="1" />
                <span asp-validation-for="Version" class="text-danger"></span>
            </div>
            <div class="col-md-2">
                <label asp-for="Status" class="form-label"></label>
                <select asp-for="Status" asp-items="Html.GetEnumSelectList<ERP.Domain.Enums.Manufacturing.RoutingStatus>()" class="form-select"></select>
            </div>
            <div class="col-md-3">
                <label asp-for="ItemId" class="form-label">@T["Item"]</label>
                <select asp-for="ItemId" class="form-select">
                    <option value="">-- Select --</option>
                    @foreach (var option in Model.ItemOptions)
                    {
                        <option value="@option.Id">@option.Label</option>
                    }
                </select>
                <span asp-validation-for="ItemId" class="text-danger"></span>
            </div>
            <div class="col-md-3">
                <label asp-for="WorkCenterId" class="form-label">@T["Work Center"]</label>
                <select asp-for="WorkCenterId" class="form-select">
                    <option value="">-- Select --</option>
                    @foreach (var option in Model.WorkCenterOptions)
                    {
                        <option value="@option.Id">@option.Label</option>
                    }
                </select>
                <span asp-validation-for="WorkCenterId" class="text-danger"></span>
            </div>
            <div class="col-md-3">
                <label asp-for="TotalLeadTimeHours" class="form-label">@T["Lead Time (Hours)"]</label>
                <input asp-for="TotalLeadTimeHours" class="form-control" type="number" min="0" step="0.01" />
                <span asp-validation-for="TotalLeadTimeHours" class="text-danger"></span>
            </div>
            <div class="col-md-3 d-flex align-items-center">
                <div class="form-check mt-4">
                    <input asp-for="IsActive" class="form-check-input" />
                    <label asp-for="IsActive" class="form-check-label">@T["Active"]</label>
                </div>
            </div>
            <div class="col-md-12">
                <label asp-for="Notes" class="form-label"></label>
                <textarea asp-for="Notes" class="form-control" rows="2"></textarea>
            </div>
        </div>

        <div class="mt-4 d-flex gap-2">
            <button type="submit" class="btn btn-primary">@T["common.save"]</button>
            <a asp-action="Routings" class="btn btn-outline-secondary">@T["common.back"]</a>
        </div>
    </div>
</div>
